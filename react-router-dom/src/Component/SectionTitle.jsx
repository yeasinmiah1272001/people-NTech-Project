import React from 'react';

const SectionTitle = ({ title}) => {
       return (
         <div>
           <h1 className="text-red-500 font-bold text-3xl mt-4">{title}</h1>
         
         </div>
       );
};

export default SectionTitle;